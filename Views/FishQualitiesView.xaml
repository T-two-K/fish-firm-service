<UserControl x:Class="ProgrammingPractice_L19.Views.QualitiByVoyagesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ProgrammingPractice_L19.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="auto"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <Border Background="Beige">
            <TextBlock Text="Все качества рыб и их рейсы" Grid.ColumnSpan="2"
                       FontWeight="Bold" FontSize="30" Margin="0 5 0 0"/>
        </Border>

        <Border Grid.Column="0" Grid.Row="1" Background="Beige" >
            <DataGrid ItemsSource="{Binding QualityCatch}" Background="Bisque"
                   AutoGenerateColumns="False"
                   CanUserAddRows="False" FontSize="16"
                   RowBackground="Beige"
                   AlternatingRowBackground="BurlyWood" Margin="5"
                   IsReadOnly="True"
                   SelectionMode="Single">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Качество" Binding="{Binding Quality}" Width="*"/>
                    <DataGridTextColumn Header="Номер рейса" Binding="{Binding VoyageNumber}" Width="*"/>
                    <DataGridTextColumn Header="Общий улов" Binding="{Binding TotalCatch}" Width="*"/>
                    <DataGridTextColumn Header="Дата начала" Binding="{Binding StartDate,
                        Converter={StaticResource DateToString}}" Width="*"/>
                    <DataGridTextColumn Header="Дата конца" Binding="{Binding EndDate,
                        Converter={StaticResource DateToString}}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
