<UserControl x:Class="ProgrammingPractice_L19.Views.VoyagesByJarAndFishView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ProgrammingPractice_L19.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="auto"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <Border Background="Beige">
            <Grid Grid.Row="0" Grid.Column="0"
              Margin="5" Background="Bisque">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"></RowDefinition>
                    <RowDefinition Height="*"></RowDefinition>
                    <RowDefinition Height="*"></RowDefinition>
                </Grid.RowDefinitions>
                <TextBlock Text="Рейс по качеству и банке" Grid.ColumnSpan="2"
                           FontWeight="Bold" FontSize="30" Margin="0 5 0 0"/>
                <StackPanel Grid.Row="1" Grid.Column="0" Margin="5">
                    <TextBlock Text="Качество" Margin="3"/>
                    <ComboBox ItemsSource="{Binding Qualities}" 
                          SelectedItem="{Binding SelectedQuality}">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding}"/>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                </StackPanel>
                <StackPanel Grid.Row="1" Grid.Column="1" Margin="5">
                    <TextBlock Text="Её средний улов" Margin="3"/>
                    <ComboBox ItemsSource="{Binding Jars}" 
                              SelectedItem="{Binding SelectedJar}">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Name}"/>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                </StackPanel>
                <Button Grid.ColumnSpan="2" Grid.Row="2" MaxWidth="150" Margin="5"
                        Content="Подтвердить" Foreground="Bisque"
                        Command="{Binding ShowVoyagesCommand}"/>
            </Grid>
        </Border>

        <Border Grid.Column="0" Grid.Row="1" Background="Beige" >
            <DataGrid ItemsSource="{Binding VoyagesByJarQal}" Background="Bisque"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False" FontSize="16"
                  RowBackground="Beige"
                  AlternatingRowBackground="BurlyWood" Margin="5"
                  IsReadOnly="True"
                  SelectionMode="Single">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Рейс" Binding="{Binding VoyageName}" Width="*"/>
                    <DataGridTextColumn Header="Рыба" Binding="{Binding FishName}" Width="*"/>
                    <DataGridTextColumn Header="Вес" Binding="{Binding Weight}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
