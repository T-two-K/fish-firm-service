<Window x:Class="ProgrammingPractice_L19.Views.EnterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ProgrammingPractice_L19.Views"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Title="WindowLogin" Height="550" Width="500">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.5*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="0.3*"></RowDefinition>
            <RowDefinition Height="0.7*"></RowDefinition>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="Aqua" Margin="10" CornerRadius="30">
            <TextBlock Text="LOGIN" FontSize="30"
                   TextAlignment="Center" VerticalAlignment="Center" FontWeight="Bold"/>
        </Border>

        <Border Grid.Row="1" Background="Aquamarine" CornerRadius="20" Margin="10">
            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="Login" FontSize="17" Margin="15"/>
                <TextBox Text="{Binding TextBoxLogin,
                    UpdateSourceTrigger=PropertyChanged}" Margin="15" MinHeight="30" FontSize="17"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="2" Background="Aquamarine" CornerRadius="20" Margin="10">
            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="Password" FontSize="17" Margin="15"/>
                <TextBox Text="{Binding TextBoxPassword,
                    UpdateSourceTrigger=PropertyChanged}" Margin="15" MinHeight="30" FontSize="17"/>
            </StackPanel>
        </Border>
        <TextBlock Grid.Row="3" Text="{Binding ErrorMessage}" Foreground="Crimson"/>
        <Border Grid.Row="4" Background="Beige" CornerRadius="20" Margin="10">
            <Button Grid.Row="1" 
                    HorizontalAlignment="Center" VerticalAlignment="Center" Background="Bisque"
                    MinHeight="50" MinWidth="150" Content="Login" Command="{Binding LoginCommand}"
                    IsDefault="True"/>
        </Border>
    </Grid>
</Window>
